# Session: 2026-08-06 — Income calculator, UI polish, TWAP fix, publish

## Summary
Shipped a Subscribe-page income projection calculator, polished the dashboard/header UI,
fixed the on-chain `claimToken` TWAP blocker, and republished to GitHub Pages. Project is
~98% done — live on BSC mainnet + cashonchain.network, remaining items are operational.

## What Was Done
- **Income Calculator** (new `app/src/sections/IncomeCalculator.jsx`, wired into `sections/Subscribe.jsx`
  below Entry Packages): inputs = directs/member, levels deep (max 12), package (6 tiers). Network grows
  as `directs^level`; projects every reward stream (direct referral 5%, direct passive 50%→200%,
  line income 10%×10, override 0–5%, daily passive, 1% product) and summarizes **total network** +
  **total income projected**. Works pre-wallet. Styled in `overrides.css` (`.coc-calc`), Orbitron figures.
- **Dashboard user count** (`sections/Dashboard.jsx`): Orbitron digital font, theme-accent color (added
  `.coc-usercount` to all 6 `theme-colors/*.css` so it tracks the active theme), bold solid-white "USERS".
- **Header** (`components/StaticHeader.jsx`, `pages/HomeLayout.jsx`): "CASH ON CHAIN" brand centered,
  mobile-only; burger menu open by default (closes only via X); social links hidden; Subscribe nav icon
  → bell (`data/site.js`).
- **On-chain fix:** ran `growObservationCardinality(100)` on the COCT/USDT LP manager
  (`0xBf946e…10B9`, signer = LP owner `0xE1C2…8dbE`) — pool `observationCardinalityNext` 1 → 100. This
  unblocks `getTwapPriceX96()` (was reverting `TwapPriceFailed`) once ~10 min of swap history accrues,
  fixing `claimToken()` `UNDELIVERABLE`.
- **Published** to `gh-pages` on `dlysen-sys/cashonchain` + **committed** source (`d3474f3`, main).

## Current State
- **LIVE:** BSC mainnet contracts + frontend at cashonchain.network. dApp functional (rewards is an
  accounts admin).
- **TWAP warm-up pending:** buffer grows lazily — needs a few swaps + ~10 min before `claimToken` swap
  path is fully live. Interim fallback = seed COCT reserve + `setProductRate` to market (~$0.16 → 6.135e18).
- Source commit `d3474f3` is **local only** (not pushed to origin/main) — gh-pages site is already updated.

## Files Modified
- `app/src/sections/IncomeCalculator.jsx` (new) · `app/src/sections/Subscribe.jsx` (wire)
- `app/src/sections/Dashboard.jsx` · `app/index.html` (Orbitron font link)
- `app/public/css/theme-colors/{blue,green,orange,pink,purple,red}.css` (`.coc-usercount`)
- `app/src/overrides.css` (`.coc-calc`, `.coc-usercount`, `.header-brand`)
- `app/src/components/StaticHeader.jsx` · `app/src/pages/HomeLayout.jsx` · `app/src/data/site.js`

## Next Session / Residual (~2%)
- Warm the TWAP (swaps + 10 min) OR seed COCT reserve + `setProductRate` — then retry `claimToken`.
- Push source commit `d3474f3` to `origin/main` if desired.
- Operational hardening: 4 distinct split wallets (currently all owner EOA), demote old rewards
  `0x83Cd…` from accounts admin, owner → multisig.

## Time Spent
~1 session (continued)
