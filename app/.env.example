# Reown (WalletConnect) project id — get one at https://dashboard.reown.com
# Public client identifier (safe in the frontend). Falls back to a shared dev id if unset.
VITE_REOWN_PROJECT_ID=your_reown_project_id_here
