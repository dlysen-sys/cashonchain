# Archived section bodies

Initial scaffold section files (`*.html` and their `.jsx` wrappers) land here as each section
is replaced with real Cash On Chain (COC) content.

See `src/pages/archived/README.md` for the full archive-on-replace workflow. Files here are
reference only — not imported by the live app.
